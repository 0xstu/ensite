export const shortenCardAddress = (address) => (`${address.slice(0, 15)}...${address.slice(address.length - 5)}`);
export const shortenAddress = (address) => (`${address.slice(0, 5)}...${address.slice(address.length - 4)}`);